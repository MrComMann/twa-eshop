<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Administration</title>
    <script>
        async function sendReq(url, method, headers = null, data = null) {
            const options = { method: method, headers: { "Content-type": "application/json", ...headers } }
            if (method != "GET" && data != null) options.body = JSON.stringify(data);
            const res = await fetch(url, options)
            return res;
        }

        async function loginHandle(e) {
            e.preventDefault()
            const username = document.getElementById("username").value
            const password = document.getElementById("password").value
            const result = await sendReq('http://localhost:3000/auth/login', "POST", null, { username, password });

            /*
            if (result.status == 401 || result == null) {
                console.log('Bad password')
            } else {
                const token = JSON.parse(await result.text());
                let stringedToken = JSON.stringify(token)
                sessionStorage.setItem("token", stringedToken);
                await sendReq('http://localhost:3000/administration/home', "POST", { "Authorization": stringedToken }, null);
            }
*/
        }
    </script>
</head>

<body>
    <form onsubmit="loginHandle(event)">
        <input type="text" id="username"><input type="password" id="password"><input type="submit">
    </form>
</body>

</html>